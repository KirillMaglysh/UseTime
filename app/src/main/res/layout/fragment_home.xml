<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".ui.home.HomeFragment">

    <include
            android:id="@+id/hero_state_layout"
            layout="@layout/hero_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1"
    />

    <TextView
            android:id="@+id/motivation_quote"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="''Конфуций основал религию и помогал пандам, а что сделал ты?''"
            android:textStyle="italic"
            android:textSize="18dp"
            android:layout_marginStart="70dp"
            android:layout_marginEnd="10dp"
    />

    <!-- TODO Заменить на текущую статистику за день-->
    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_weight="3"
    >

        <include
                android:id="@+id/short_summary_1"
                layout="@layout/short_summary_card"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
        />

        <include
                layout="@layout/short_summary_card"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
        />
    </LinearLayout>

    <!--
        <include
                android:id="@+id/home_progress_window"
                layout="@layout/progress_window"
                android:visibility="invisible"
        />
    -->
</LinearLayout>